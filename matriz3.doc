Derivadas Parciais de f(x, y) = exp(x² + 2y²)

Primeiras Derivadas:

    Em relação a x: f_x = 2x * exp(x² + 2y²)
    Em relação a y: f_y = 4y * exp(x² + 2y²)

Segundas Derivadas:

    f_xx: 2 * exp(x² + 2y²) * (1 + 2x²)
    f_yy: 4 * exp(x² + 2y²) * (1 + 4y²)
    f_xy = f_yx: 8xy * exp(x² + 2y²)